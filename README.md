# dataportal-blocks

EntryScape Blocks configuration project, scaffolded by
`blocks-bundler create`.

## Layout

- `src/`   — configuration sources (config, collections, style, blocks).
              See `src/README.md`.
- `dist/`  — built bundles. Generated by `blocks-bundler`; do not edit.
- `demo/`  — minimal demo page that loads the bundle via the
              EntryScape Blocks CDN.

## Scripts

- `pnpm start` — run `dev` and `serve` together (rebuild + auto-reload).
- `pnpm dev`   — watch `src/` and rebuild into `dist/` only.
- `pnpm build` — single build (readable + minified) into `dist/`.
- `pnpm serve` — serve `demo/index.html` on :5173 and reload the
                  browser when `dist/*.js` changes (no rebuild).

Typical flow: `pnpm install && pnpm start`, then open
<http://localhost:5173/demo/>.
